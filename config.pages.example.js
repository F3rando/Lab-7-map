// For GitHub Pages: copy this file to `config.pages.js`, paste your Mapbox *public* token,
// and commit `config.pages.js` so it is deployed (unlike gitignored `config.local.js`).
// In Mapbox, add a URL restriction for your Pages site, e.g. https://YOURUSER.github.io/*
export const MAPBOX_ACCESS_TOKEN = '';
